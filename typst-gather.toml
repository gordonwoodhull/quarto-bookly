# typst-gather configuration
# Run: quarto call typst-gather

rootdir = "_extensions/bookly"
destination = "typst/packages"

discover = ["numbering.typ", "typst-show.typ", "page.typ"]

# Local packages (@local namespace) must be configured manually.
# Found @local imports:
#   @local/bookly:1.1.3 (in typst-show.typ)
[local]
bookly = "/Users/gordon/src/tb-work/templates/bookly"
